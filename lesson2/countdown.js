// Regular chalenge 2 : loop to console log a variable number, decrease it by one, and log that number, 
//and stop after the number prints 0;
function countdown() {
    for (var i=10;i >= 0;i--) {
      console.log(i);
    }
  }
  countdown();